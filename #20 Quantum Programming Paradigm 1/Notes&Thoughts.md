## About the code
The code does a pretty straightfoward get-all-the-possible-string-and-the-do-the-simulate thing. The $1/\sqrt(2)$ is a pain to do so I just count the $1/\sqrt(2)$s and multiply them at the beginning.

## About the 90-degree words at the side
I didn't do the I part yet but I can tell that it is more difficult. As the I part act more like a real manipilation, the bits only have to behave like a certain bit and every change need only to follow the probility. Part II requires an overall controll to the bits, and n, the number of qubits is restricted to the memory of the computer. 