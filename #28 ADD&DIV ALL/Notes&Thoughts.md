## Notes
In this class, the Prof showed that if we do the following:
```aiignore
HAD ALL
If f(all) then minus
HAD ALL
```
then the amp of `|00...0>` bit will turn into the average of the +-1 truth table of the function, and if we extract all and get |00...0>`, we can say for sure taht the function is baised, otherwise we say `NO COMMENT`. It's the Deutsch-Josza alg.

## Solution
`(i):`
```aiignore
000 0
001 0
010 0
011 0
100 0
101 0
110 1
111 1
```
$$
\text{ExpectationGain(1)}=\frac{1}{2} 
$$
$$
\text{ExpectationGain(2)}=\frac{1}{2}
$$
$$
\text{ExpectationGain(3)}=0
$$

`(ii)` Well, it's kinda hard to draw the fig here